# REPLICATE_API_TOKEN: Required. All image models route through Replicate.
# Kept server-side; used only by the /api/replicate Vercel serverless function.
# Create a token at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN="MY_REPLICATE_API_TOKEN"
